using UnityEngine;

public class test03_04 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        string name = "PlayerName";
        name = name + ":Takeshi";
        Debug.Log(name);
        name = name + " honda";
        Debug.Log(name);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
