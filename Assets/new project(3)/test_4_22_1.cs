using UnityEngine;

public class test03_02 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
        int counter = 15;
        float time = 1.5f;
        string Enemy = "Enemy";
        Debug.Log(counter);
        Debug.Log(time);
        Debug.Log(Enemy);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
